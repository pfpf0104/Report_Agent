# 버전은 실제 로컬 개발 환경에서 기동·렌더링·마이그레이션까지 검증한 값으로 고정했다.
# matplotlib/seaborn/pytest는 아직 코드에서 쓰지 않아 검증하지 못했으므로 최신 안정 버전을
# 그대로 두었다 — 차트/테스트 코드를 추가할 때 실제 설치해서 확인 후 필요하면 조정한다.

# --- Web / API ---
fastapi==0.141.1
uvicorn[standard]==0.52.0
python-dotenv==1.2.2
pydantic==2.13.4
pydantic-settings==2.14.2

# --- Database ---
sqlalchemy==2.0.51
alembic==1.18.5
psycopg2-binary==2.9.12

# --- Rendering ---
jinja2==3.1.6
weasyprint==69.0

# --- Computation ---
numpy==2.4.6
pandas==3.0.5
scikit-learn==1.9.0
matplotlib==3.10.0
seaborn==0.13.2

# --- Ingestion (외부 API 클라이언트) ---
httpx==0.28.1
apscheduler==3.11.3

# --- Test ---
pytest==8.3.4
